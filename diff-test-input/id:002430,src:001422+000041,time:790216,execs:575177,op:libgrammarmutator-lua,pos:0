debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); debug : setmetatable(io); d[[
]]; 